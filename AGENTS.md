# idphoto — Codex 施工规则

## 开工第一件事

读 `PLAN.md`（软链到 Obsidian 库里的开发规划），那是本项目唯一的施工图。里面的技术栈、算法规格、排版数量表、项目结构都已定稿，**按它执行，不要自作主张换方案**。

`PLAN.md` 是软链接，只读。有异议在对话里提出，不要直接改它。

## 边界

- 只做 `PLAN.md` 里当前阶段的事，不要提前实现后面阶段的功能
- 一个阶段做完就停下来报告，等确认后再继续
- 不要一次性生成整个项目骨架，按文件推进
- `core/` 目录里禁止出现任何 `PySide6` import，核心逻辑必须能脱离界面在 Mac 上跑测试

## 已定死不要改的

- Python 3.11（不要升 3.12/3.13，MediaPipe 和 onnxruntime 的 wheel 会缺）
- 只装 `onnxruntime`，绝对不要装 `onnxruntime-gpu`（共存会导致 DLL 加载失败）
- mm↔px 转换只允许存在于 `core/units.py`，其他地方一律调用它，不许手写 `/25.4*300`
- 排版数量必须由求解函数算出，不许硬编码 `PLAN.md` 里那张表的数字（表只作验收基准）

## 输出风格

简洁。不要在回复里重复贴大段已经写进文件的代码，不要写冗长的方案说明和总结。做了什么、跑了什么测试、结果如何，几句话说完。
